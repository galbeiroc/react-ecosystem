import React from 'react';
import { hot } from 'react-hot-loader';

import './App.css';

const App = () => {
  return (
    <div className='App'>
      <h2>Hello, world</h2>
    </div>
  )
};

export default hot(module)(App);
